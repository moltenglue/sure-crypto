# ===========================================================================
# Rotki Integration Compose File
# ===========================================================================
#
# Usage:
#   docker-compose -f compose.example.yml -f compose.rotki.yml up -d
#
# This adds the Rotki service to the standard Sure stack

services:
  rotki:
    image: rotki/rotki:latest
    container_name: sure-rotki
    ports:
      - "5042:80"    # nginx proxy (for web UI)
      - "4242:4242"  # direct backend API (for persistent sessions)
    volumes:
      - rotki-data:/data
    environment:
      - ROTKI_IS_HEADLESS=true
      - ROTKI_REST_API_PORT=4242
    restart: unless-stopped
    healthcheck:
      test: [ "CMD-SHELL", "curl -f http://localhost/api/1/ping || exit 1" ]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 60s
    networks:
      - sure_net

volumes:
  rotki-data:
